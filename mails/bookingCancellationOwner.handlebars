{{> components/header}}
  <div class="title">
       Booking Cancellation Request
  </div>

    <div class="normal-text">
An owner has requested to cancel the booking. Check for backup assets if applicable, and proceed accordingly.    </div>

  <div class="actions">
      <div class="shadow">
          <a href="{{link}}" class="normal-btn">Go to order</a>
      </div>
  </div>
{{> components/footer}}