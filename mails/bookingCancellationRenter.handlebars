{{> components/header}}
  <div class="title">
       Booking Cancellation Request
  </div>

    <div class="normal-text">
        A user has requested to cancel the booking. Review the cancellation charges and confirm to proceed.
    </div>

  <div class="actions">
      <div class="shadow">
          <a href="{{link}}" class="normal-btn">Go to order</a>
      </div>
  </div>
{{> components/footer}}